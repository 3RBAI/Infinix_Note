# 🚀 Infinix NOTE 50X 5G - فك قفل البوت لودر (Bootloader Unlock) والروت (Root)

**المطور:** عبد العزيز الحمداني (Abdulaziz Al-Hamdani)
**المشروع:** Infinix_Note_Pro
**الإصدار:** 1.0.0
**تاريخ آخر تحديث:** 2026-01-04

تهانينا على إنجاز فك قفل البوت لودر لهذا الجهاز الجديد! هذا المستودع هو نسخة منظمة وموثقة للملفات والخطوات اللازمة لفتح البوت لودر والحصول على صلاحيات الروت الكاملة (Superuser) لهاتف **Infinix NOTE 50X 5G** (رقم الموديل: X6857).

---

## ⚠️ تحذيرات وإخلاء مسؤولية هام جداً

> **فك قفل البوت لودر هي عملية عالية المخاطر. أنت تتحمل المسؤولية الكاملة عن أي ضرر قد يلحق بجهازك.**

| التحذير | التفصيل |
| :--- | :--- |
| **فقدان البيانات** | فك قفل البوت لودر سيؤدي إلى **مسح جميع البيانات** على جهازك (Factory Reset). **يجب عمل نسخة احتياطية كاملة** قبل البدء. |
| **إلغاء الضمان** | قد يؤدي فك القفل إلى **إلغاء ضمان** الشركة المصنعة (Infinix). |
| **مشاكل الأمان** | تعطيل التحقق من التشغيل (Verified Boot) يقلل من مستوى أمان جهازك. |
| **خطر التوقف (Bricking)** | استخدام ملفات خاطئة أو تنفيذ خطوات غير صحيحة قد يؤدي إلى توقف الجهاز عن العمل (Soft/Hard Brick). |

---

## 🛠️ المتطلبات الأساسية

1.  **جهاز Infinix NOTE 50X 5G** مشحون بنسبة لا تقل عن 70%.
2.  **جهاز كمبيوتر** (Windows/macOS/Linux) مع تثبيت **ADB و Fastboot**.
3.  **تعريفات MediaTek (MTK Drivers)** مثبتة على الكمبيوتر.
4.  **ملفات النظام الأساسية (Firmware)**: متوفرة في مجلد `Firmware`.
5.  **ملف Magisk APK** (للحصول على الروت).

---

## 📝 الخطوات التفصيلية لفك قفل البوت لودر

### المرحلة الأولى: إعداد الجهاز

1.  **تمكين خيارات المطور (Developer Options):**
    *   اذهب إلى **الإعدادات** > **حول الهاتف**.
    *   اضغط على **رقم البناء (Build Number)** سبع (7) مرات متتالية حتى تظهر رسالة "أنت الآن مطور!".
2.  **تمكين تصحيح أخطاء USB وفتح قفل OEM:**
    *   اذهب إلى **الإعدادات** > **النظام** > **خيارات المطور**.
    *   فعّل خيار **تصحيح أخطاء USB (USB Debugging)**.
    *   فعّل خيار **فتح قفل OEM (OEM Unlocking)**.

### المرحلة الثانية: فك القفل عبر Fastboot

1.  **الاتصال بالكمبيوتر:** قم بتوصيل الهاتف بالكمبيوتر باستخدام كابل USB أصلي.
2.  **فتح موجه الأوامر (CMD/Terminal):** افتح موجه الأوامر في مجلد يحتوي على ملفات ADB و Fastboot.
3.  **الدخول إلى وضع Fastboot:**
    ```bash
    # لإعادة تشغيل الجهاز إلى وضع Fastboot
    adb reboot bootloader
    ```
4.  **التحقق من الاتصال:** تأكد من أن الجهاز متصل بشكل صحيح.
    ```bash
    # يجب أن يظهر رقم تسلسلي للجهاز
    fastboot devices
    ```
5.  **تنفيذ أمر فك القفل:**
    ```bash
    # الأمر الأساسي لفك قفل البوت لودر
    fastboot flashing unlock
    ```
6.  **التأكيد على الجهاز:** ستظهر رسالة تحذير على شاشة الهاتف. استخدم أزرار الصوت للتنقل واضغط على زر الطاقة لاختيار **"Unlock the bootloader"** (فك قفل البوت لودر).
7.  **إعادة التشغيل:** سيقوم الجهاز بإعادة التشغيل تلقائياً، وسيتم مسح جميع البيانات.

---

## 📂 هيكل المستودع المنظم

لقد تم إعادة تنظيم المستودع ليكون أكثر وضوحاً:

| المجلد/الملف | الوصف |
| :--- | :--- |
| `Firmware/` | يحتوي على ملفات النظام الأساسية (Stock ROM) لجهاز Infinix NOTE 50X 5G. |
| `tools/` | يحتوي على أدوات مساعدة وموديلات Magisk مثل `BootloopSaver-important.zip` و `LSPosed.zip`. |
| `docs/` | يحتوي على توثيق مفصل للملفات الفردية (مثل `FILE_DOCUMENTATION.md`) والملاحظات التقنية. |
| `scripts/` | مخصص لأي سكربتات مساعدة (مثل سكربتات فك الضغط أو الاستعادة) في المستقبل. |
| `LICENSE` | ترخيص المشروع. |
| `Credits.txt` | ذكر المصادر التي تم الحصول منها على ملفات النظام. |

---

## ⚖️ حقوق الطبع والنشر والمصادر

**المطور:** عبد العزيز الحمداني (Abdulaziz Al-Hamdani)

**الترخيص:** هذا المشروع مرخص بموجب ترخيص **MIT License**.

**المصادر:** تم الحصول على ملفات النظام الأساسية من المصادر المذكورة في ملف `Credits.txt`.

**للتواصل مع المطور:**
*   GitHub: [3RBAI](https://github.com/3RBAI)
*   Telegram: [t.me/a3b6iii](https://t.me/a3b6iii)
*   WhatsApp: [+96895596569](https://wa.me/+96895596569)
*   Email: wolfonlyoman@gmail.com
